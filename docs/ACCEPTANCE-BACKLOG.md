# 验收清欠清单

历史迭代的「验收待执行」盘点结果：**大部分已于 2026-08-17 批量验收通过**（迭代 2/3/4/7/8/8B-8D/13/14/15/16，见 ROADMAP 状态总览），各迭代章节正文中的「待执行」字样为当时的历史记录。当前真正未闭环的验收项如下，完成一项划掉一项并回写 ROADMAP 对应行。

## 1. PDA 真机链路（迭代 5 立项 → 迭代 25 排期）

- [ ] PDA 网页 → Server → PDA 宿主 → IP 打印机打出物料码；
- [ ] 开机自启、前台服务常驻、厂商 ROM 保活；
- [ ] Android 16 的 16KB 页适配（SQLitePCLRaw / libe_sqlite3）真机构建验证。

## 2. Zebra 打印机状态字段真实设备确认（DESIGN 未决）

- [ ] `~HS` 状态解析的字段映射（缺纸 / 卡纸 / 打印头温度）与 Zebra SDK 3.x `PrinterStatus` 语义，接真实 Zebra 打印机确认 `GET /api/printer/status` 展示是否准确。

## 3. Niimbot 蓝牙插件真机（迭代 26，已排期）

- [ ] 连接 / 打印 / 状态 / 异常恢复真机验收（随迭代 26 实施执行）。

## 4. Excel 导入续填冒烟（TemplateFrame 2.0.0 升级后，2026-08-25）

- [ ] 客户端「数据与打印」下载 Excel 模板 → Excel 中在示例行下方续填多行 → 导入，确认预览列表行数完整（1.0.5 命名区域 EndRow 限制曾导致续填行被静默丢弃；自动化测试覆盖不到手工续填的文件形态）。

## 5. 净机安装冒烟（低频，发大版本前抽查）

- [ ] 干净电脑（无 .NET 10 Desktop Runtime）：装 Server + Client MSI，确认运行时缺失弹窗与下载链接可点、装完即可用；
- [ ] 覆盖升级保留用户配置（appsettings.json / connection.json / settings.json）、卸载可选清数据。

## 6. 安装向导视觉验收（迭代 31，本地 0.21.0 测试包）

- [ ] Client / Server MSI 全新安装：欢迎页（品牌左幅）→ 许可 → 安装目录（默认 Program Files，可改）→ 安装 → 完成页；Client 完成后仍有「立即打开」TopMost 弹窗
- [ ] 运行时缺失场景：提示对话框出现在向导之前
- [ ] 卸载：清数据询问 → 维护向导；勾选清除后数据目录被清（含自定义安装目录场景）

> 注：清单外新增功能（如后续迭代）验收在各自迭代内闭环，不进入本清单。
