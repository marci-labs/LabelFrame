# 验收清欠清单

历史迭代的「验收待执行」盘点结果：**大部分已于 2026-08-17 批量验收通过**（迭代 2/3/4/7/8/8B-8D/13/14/15/16，见 ROADMAP 状态总览），各迭代章节正文中的「待执行」字样为当时的历史记录。当前真正未闭环的验收项如下，完成一项划掉一项并回写 ROADMAP 对应行。

## 1. PDA 真机链路（迭代 5 立项 → 迭代 25 完成主体验收）

- [x] PDA 网页 → Server → PDA 宿主 → IP 打印机打出物料码。2026-09-08 迭代 25 真机验收（UROVO DT50 / Android 11）：注册 / 心跳 / 模板下发 / 作业打印 / 离线恢复 / 断网重连 / 开机自启 / 前台服务保活 / JS 桥 CORS 全部通过；出纸链路以本机 TCP9100 模拟打印机字节级验证（^PW480/^LL320 精确换算、^GF 位图 QR 定位角与中文文本可见）。**真实 IP 打印机物理出纸与扫码枪可扫性仍待验收**（需现场打印机，见下条）。
- [x] 真实 IP 打印机（TCP 9100）物理出纸（迭代 25 收尾新增）。2026-09-08 用户确认：Zebra（192.168.2.121）经 PDA 直连作业正确出纸（60×40mm 物料标签：中文 + 二维码），PDA 侧状态探测在线 / 有纸 / 未暂停。扫码枪逐张抽检未执行（用户确认初版可用），后续按试点验收流程补。
- [x] 开机自启、前台服务常驻、厂商 ROM 保活。2026-09-08 验证：重启后 10s 内 BootReceiver 拉起前台服务（isForeground=true），UROVO 默认 ROM 策略下常驻；其他厂商 ROM 差异后续按需抽查。
- [x] Android 16 的 16KB 页适配（SQLitePCLRaw / libe_sqlite3）真机构建验证。2026-09-08 构建级通过：`lib.e_sqlite3.android` 定版 2.1.11（NDK 构建、LOAD 段 16KB 对齐），APK 全部 arm64 so 段对齐 ≥ 16KB，`zipalign -c -P 16` 通过，无 XA0141。**运行时验证待 Android 15+ 16KB 内核设备**（DT50 为 Android 11 / 4KB 页）。

## 2. Zebra 打印机状态字段真实设备确认（DESIGN 未决）

- [ ] `~HS` 状态解析的字段映射（缺纸 / 卡纸 / 打印头温度）与 Zebra SDK 3.x `PrinterStatus` 语义，接真实 Zebra 打印机确认 `GET /api/printer/status` 展示是否准确。

## 3. Niimbot 蓝牙插件真机（已取消）

- [ ] ~~连接 / 打印 / 状态 / 异常恢复真机验收（随迭代 26 实施执行）。~~
  —— 2026-09-07：迭代 26 已放弃（PDA 宿主走 IP 打印、无蓝牙承接需求，见 ROADMAP），本项随之取消，不再承接。

## 4. Excel 导入续填冒烟（TemplateFrame 2.0.0 升级后，2026-08-25）

- [x] 客户端「数据与打印」下载 Excel 模板 → Excel 中在示例行下方续填多行 → 导入，确认预览列表行数完整。2026-08-28 实测：原文件命名区域 `TF_Table='打印数据'!$A$1:$A$2` 保持不变，在示例行下续填 A3:A5 后，管理界面导入对话框识别为「4 行数据」并提供「批量打印 4 张」，证明 2.0.0 不再受命名区域 EndRow 限制。

## 5. 净机安装冒烟（低频，发大版本前抽查）

- [ ] 干净电脑（无 .NET 10 Desktop Runtime）：装 Server + Client MSI，确认运行时缺失弹窗与下载链接可点、装完即可用；
- [ ] 覆盖升级保留用户配置（appsettings.json / connection.json / settings.json）、卸载可选清数据。

## 6. 安装向导视觉验收（迭代 31，本地 0.21.0 测试包）

- [ ] Client / Server MSI 全新安装：欢迎页（品牌左幅）→ 许可（标题无字体名混入）→ 安装目录（默认 Program Files，可改）→ 安装 → 完成页；Client 完成页复选框「立即打开 LabelFrame」默认勾选、点完成后启动客户端
- [ ] 向导各页按钮 / 引导文字全部中文；Client「安装选项」页「开机自动启动」默认勾选，安装后重启系统验证托盘自启（不弹浏览器）；取消勾选安装则无自启
- [ ] 运行时缺失场景：提示对话框出现在向导之前
- [ ] 卸载：清数据询问 → 维护向导；勾选清除后数据目录被清（含自定义安装目录场景）

> 注：清单外新增功能（如后续迭代）验收在各自迭代内闭环，不进入本清单。
