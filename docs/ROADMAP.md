# LabelFrame 路线图

> 迭代索引与状态总览。**立项 / 执行 / 验收在 GitHub Issue**（「迭代任务」模板立项，链接见下表）；流程见 [WORKFLOW.md](WORKFLOW.md)。
> 迭代 0-41 的目标 / 范围 / 验收详情：[archive/ROADMAP-ITERATIONS.md](archive/ROADMAP-ITERATIONS.md)；迭代 42 起详情在对应 Issue。
> 设计细节见 [DESIGN.md](DESIGN.md)，需求见 [REQUIREMENTS.md](REQUIREMENTS.md)。

## 状态总览

| 迭代 | 主题 | 状态 |
|---|---|---|
| 0 | 奠基：文档体系 + 解决方案骨架 | ✅ 已完成 |
| 1 | 契约与 ZPL | ✅ 已完成 |
| 2 | WinHost 打印闭环 | ✅ 已完成（2026-08-17 真实设备验收通过） |
| 3 | Server 路由 | ✅ 已完成（2026-08-17 真实设备验收通过） |
| 4 | 模板管理 + 预览 | ✅ 已完成（2026-08-17 真实设备抽查通过） |
| 5 | PDA 宿主 | ✅ 已完成（真机验收待执行） |
| 6 | P1 收尾 | ✅ 已完成（蓝牙 / Android 项随迭代 5 受阻） |
| 7 | Studio 模板工具（V1） | ✅ 已完成（2026-08-17 界面验收通过） |
| 8 | Studio 版式编排（V2） | ✅ 已完成（2026-08-17 界面验收通过） |
| 8B | Studio 版式增强（字段编辑 / 元素样式 / 区域布局） | ✅ 已完成（2026-08-17 界面验收通过） |
| 8C | Studio 界面重构（工作台 + 设计器） | ✅ 已完成（2026-08-17 界面验收通过） |
| 8D | 设计器交互重做（容器 / 设计测试分离 / 字段自动推导 / 标尺对齐 / 多选手柄） | ✅ 已完成（2026-08-17 界面验收通过） |
| 8E | Web 设计器原型 v2（视口缩放 / 条码二维码实时渲染 / 智能参考线 / 文本溢出模式） | ✅ 已完成 |
| 8F | Web 设计器原型 v3（画布留白 + 标尺 / 真实比例 1mm=8点 / 边界约束 / 拖入修复） | ✅ 已完成 |
| 9 | Excel 数据导入 | ✅ 已完成 |
| 10 | MSI 安装包 | ✅ 已完成 |
| 11 | 单机模式（Host 服务化 + Web Vite/TS 前端 + PDA 测试链路） | ✅ 已完成（单机服务化与 Web 前端主线交付，后续演进见迭代 12-24） |
| 12 | 模板预览值持久化 + 图片打印实验 | ✅ 已完成（2026-08-17 前端 renderLabelImage 取消，不再实施） |
| 13 | 文本排版与二维码参数持久化（元素契约补齐） | ✅ 已完成（2026-08-17 用户验收通过） |
| 14 | 字体加粗（bold）契约 | ✅ 已完成（2026-08-17 联调验收通过） |
| 15 | 打印设置与会话保留 + 连接管理 + 删除 ZPL（图片打印收敛） | ✅ 已完成（2026-08-17 联调验收通过） |
| 16 | 服务端 / 客户端拆分（双安装包） | ✅ 已完成（2026-08-17 用户验收通过） |
| 18 | 无头服务端 + 客户端 UI 回归 + Windows 服务 + 历史清理 + 推送通知（0.15.4） | ✅ 已完成（2026-08-17 联调验收通过） |
| 19 | Ubuntu 服务端部署 + 跨机验证（服务端 Linux / 客户端 Windows） | ✅ 已完成（2026-08-17 真机部署验收通过） |
| 20 | 服务端管理界面（插件式 UI）+ 设备 IP | ✅ 已完成（2026-08-17 联调冒烟通过） |
| 21 | 自动化发布（ghcr + GitHub Release + MSI 签名通道） | ✅ 已完成（v0.17.0 自动发布成功；ghcr 包已公开） |
| 22 | 打印测试体验 + 传输插件化 + 客户端下载分发 | ✅ 已完成（2026-08-17 迭代结束，本地 0.18.0 测试包验收） |
| 23 | 客户端插件分发——上传服务端 + 客户端安装 / 卸载 | ✅ 已完成（2026-08-17 前端完成 + loadError 补充 + 0.19.0 打包验收） |
| 24 | 客户端批次作业（Batch Print） | ✅ 已完成（2026-08-18：前后端合入 master 67214c3 + 端到端联调附五通过 + Serilog 日志命名修复） |
| 25 | Android PDA 宿主（AndroidHost） | ✅ 已完成（2026-09-08 UROVO DT50 真机验收通过） |
| 26 | Niimbot 蓝牙打印机传输插件实现 + 真机测试 | ❌ 已放弃（2026-09-07：PDA 走 IP 打印、无蓝牙承接需求，蓝牙降级待需求） |
| 27 | 工程治理 P0（日常 CI + API 契约与端点去重 + README/DEPLOY 重组） | ✅ 已完成（2026-08-25） |
| 28 | 工程治理 P1/P2（文档归档 / 死重移除 / 数据层并发 / 异常契约 / Program 拆分 / 集成测试 / 安全边界） | ✅ 已完成（2026-08-25） |
| 29 | 程序优化（SQLite WAL / 数据层基建 / 分析器门禁 / Server 集成测试 / 覆盖率收集） | ✅ 已完成（2026-08-25） |
| 30 | 注释与文档清理（代码去过程化 / DESIGN 重构） | ✅ 已完成（2026-08-25） |
| 31 | 安装包 UI 专业化（WixUI 向导 / 品牌位图 / ARP 元数据） | ✅ 已完成（2026-08-25，视觉验收待执行） |
| 32 | 测试完善（端点集成 / 渲染元素 / TimeProvider 确定性 / 设计器组件 / MSI 断言 CI） | ✅ 已完成（2026-08-25） |
| 33 | 性能 / 稳定性测试（微基准 / 端到端延迟 / soak / nightly CI） | ✅ 已完成（2026-08-25） |
| 34 | Linux 无头客户端（Log 驱动）+ Server / Client Compose E2E | ✅ 已完成（2026-08-28） |
| 35 | Linux Client 正式发布 + 双端稳定 Compose + P0/P1 测试补强 | ✅ 已完成（2026-08-28，v0.22.0） |
| 36 | 文档治理与路线图收口（迭代 26 放弃 / 迭代 25 转下一轮 / 一致性核对 / 注释清理） | ✅ 已完成（2026-09-07） |
| 37 | 服务端暂存作业 TTL 过期 + notify 积压即时唤醒 | ✅ 已完成（2026-09-07） |
| 38 | 测试稳定性小治理（flaky 用例加固 + 本机构建产物清理） | ✅ 已完成（2026-09-07） |
| 39 | 性能优化批次（Worker 信号量唤醒 / SQLite 写合批 / SKBitmap 池） | ✅ 已完成（2026-09-07） |
| 40 | PDA 宿主配置与测试体验（原生配置页 / 状态页收敛 / pc_host 移除） | ✅ 已完成（2026-09-08 真机冒烟通过） |
| 41 | PDA 配置界面文案与视觉优化（人话文案 / 分组重组 / 视觉层级） | ✅ 已完成（2026-09-08 真机走查通过） |
| 发布补丁 | Server Docker 中文字体基线 | ✅ 已完成（2026-09-03，v0.22.1） |
| 发布补丁 | Linux 容器中文字体切换到文泉驿微米黑 | ✅ 已完成（2026-09-07，v0.22.2） |
| 检查点 | 试点验收（成功衡量） | ✅ 已完成（2026-08-17：扫码枪 50 张 + 连续 100 张压力验证通过） |
| 42 | 工作流重构：Issue 驱动迭代 + PR 门禁（squash / 双必需检查 / ROADMAP 瘦身） | ✅ 已完成（2026-09-09 门禁演练双通过，[#1](https://github.com/marci-labs/LabelFrame/issues/1)） |
| 43 | Claimed 作业超时回收（宿主重启后终态化：Failed + LF_SRV_009，默认 30 分钟） | ✅ 已完成（2026-09-09，[#4](https://github.com/marci-labs/LabelFrame/issues/4)） |
| 44 | Windows 客户端窗口化（WebView2 嵌入界面壳 + MSI 运行时引导） | ✅ 已完成（2026-09-10 用户真机验收通过，[#8](https://github.com/marci-labs/LabelFrame/issues/8)） |
| 45 | 批次首张节奏修正 + 设置页布局自适应 + 工作台小修（批间计数按作业重置 #100 / 多列网格 / 模板名搜索 + 操作列修正） | ✅ 已完成（2026-09-10 用户真机验收通过，[#11](https://github.com/marci-labs/LabelFrame/issues/11)） |
| 46 | 工作台模板缩略图预览（预览列内嵌缩略图 + 居中灯箱放大，零契约变更） | ✅ 已完成（2026-09-10 用户真机验收通过，经三轮验收修订定稿，[#15](https://github.com/marci-labs/LabelFrame/issues/15)） |
| 47 | 作业可观测性（进度增量上报 #101/#102 + 日志细化 + 缺陷 #14 修复） | ✅ 已完成（2026-09-10 验收收尾：真机日志落盘 / 级别切换、真实 Server 进度复验（v0.24.0 升级后）通过，PDA 复验跳过（未单独测试，异常再开缺陷），[#16](https://github.com/marci-labs/LabelFrame/issues/16)） |
| 48 | 工作台信息架构重构（卡片网格定稿 #103）+ 作业历史自动轮询（1.5s / 仅进行中 / 隐藏暂停） | ✅ 已完成（2026-09-10 用户验收通过，[#27](https://github.com/marci-labs/LabelFrame/issues/27)） |
| 发布 | v0.25.0 迭代 48 发布（工作台卡片网格 / 作业历史自动轮询） | ✅ 已完成（2026-09-10，tag v0.25.0） |
| 流程治理 | 启用多会话 worktree 并行（试行，WORKFLOW §7） | ✅ 已完成（2026-09-10 首次并行演练完成并转正，记录见 WORKFLOW §7.1，[#17](https://github.com/marci-labs/LabelFrame/issues/17)） |
| 发布 | v0.24.0 迭代 40-47 汇总发布（PDA 配置与文案 / Claimed 超时回收 / 客户端窗口化 / 批次节奏与工作台小修 / 缩略图预览 / 作业可观测性） | ✅ 已完成（2026-09-10，tag v0.24.0，含跨端 progress 契约 #101 与批次节奏语义 #100） |
| 51 | 客户端设备日志链路与前端可观测（WinHost 补挂 /api/logs + 日志页失败显错 + ErrorBoundary 全局兜底 + 设备日志按行存储 #106） | ✅ 已完成（2026-09-11 用户验收通过，[#34](https://github.com/marci-labs/LabelFrame/issues/34)） |
| 50 | 错误响应分类修正（请求体失败 400 LF_API_BAD_BODY + 传输测试失败 LF_TRANSPORT_TEST_FAILED + 403 补 ErrorView + 错误码注册表 + 插件消息中文化 #107） | ✅ 已完成（2026-09-11 验收证据闭环（沙箱实测），用户委托自动结项，[#33](https://github.com/marci-labs/LabelFrame/issues/33)） |
| 53 | PDA 可观测性（logcat 日志封装 HostLog + 全局崩溃捕获 CrashGuard + 本地滚动日志 #105） | ✅ 已完成（2026-09-11 真机（DT50）取证通过，用户委托自动结项，[#36](https://github.com/marci-labs/LabelFrame/issues/36)） |
| 52 | 日志基础设施加固（FileLoggerProvider 启动防护 + 日志按日轮转与保留 #108 + Server 业务事件日志 + 杂项吞错留痕） | ✅ 已完成（2026-09-11 沙箱实证验收通过，跨天滚动随部署观察；用户委托自动结项，[#35](https://github.com/marci-labs/LabelFrame/issues/35)） |
| 54 | 区域水平锚定修正（自动宽度文本按实测宽度锚定 + 字宽度量接口 ITextWidthMeasurer 注入解析器 #110） | ✅ 已完成（2026-09-11 用户真机验收通过（预览 + 打印对比），[#44](https://github.com/marci-labs/LabelFrame/issues/44)） |
| 55 | Zebra 状态映射真机确认（~HS 字段实证修正 + 无响应一等异常态 + SDK GetCurrentStatus 细状态 #109） | ✅ 已完成（2026-09-11 真机三场景双路径取证通过（正常 / 缺纸 / 暂停），[#45](https://github.com/marci-labs/LabelFrame/issues/45)） |
| 56 | 双端复用 Zebra 官方 SDK 5.0.3685（AndroidHost 接入 tcp/蓝牙/USB + WinHost 升级统一 #111） | ✅ 已完成（2026-09-11 真机验收通过（tcp 直发 + Server 路由双路径 + WinHost Zebra 回归），蓝牙 / USB 转验收欠账，[#49](https://github.com/marci-labs/LabelFrame/issues/49)） |
| 57 | 安装引导专项 1/8——设计契约（install manifest 格式 / 拓扑预设与开关模型 / 信任模型与分发源 / 选型与签名策略 #114~#118） | ✅ 已完成（2026-09-11 纯文档契约迭代，AC 自证；专项 2/8~8/8 以此为契约依据，[#50](https://github.com/marci-labs/LabelFrame/issues/50)） |
| 57 | 客户端退出提速（事件驱动提前退出 + 500ms 稳定窗，决策 #112 修订） | ✅ 已完成（2026-09-12 带托盘沙箱实测 <1.5s，用户委托自动结项，[#64](https://github.com/marci-labs/LabelFrame/issues/64)） |
| 59 | 安装引导专项 3/8——PDA 签名稳定化 + 服务端下载中心扫码下载（pda-packages + 统一下载中心页 + 二维码，#119） | ✅ 已完成（2026-09-14 真机扫码下载 + 同签名覆盖升级验收通过（DT50，设备号不变），AC-01 发版实证随下次 v* 发版（#31 同批），[#52](https://github.com/marci-labs/LabelFrame/issues/52)） |
| 65 | 无字段模板打印测试修复（数据与打印页静态标签可打印：操作区照常渲染 + 空数据提交 + 出图预览） | ✅ 已完成（2026-09-14 沙箱浏览器自动化界面走查通过（空数据作业 202 + PNG 出图），用户委托自动结项，[#62](https://github.com/marci-labs/LabelFrame/issues/62)） |
| 发布 | v0.26.0 迭代 49-59、64、65 汇总发布（可观测性 / Zebra 官方 SDK 双端 / 下载中心扫码下载 / 退出提速与托盘修复 / 无字段模板；APK 与 install-manifest.json 首发随 Release 分发） | ✅ 已完成（2026-09-14，tag v0.26.0） |
| 58 | 安装引导专项 2/8——CI 自动生成安装清单 install-manifest.json + latest.json（生成与断言一体，#120） | ✅ 已完成（2026-09-14 v0.26.0 实跑发版验证通过（manifest 五条目 + 五产物 sha256 逐项复核 + 值守独立抽查），用户委托自动结项，[#51](https://github.com/marci-labs/LabelFrame/issues/51)） |
| 49 | PDA 宿主自动化构建与品牌化（CI Android 构建 + release APK 附件 + L 品牌图标 + 版本透出，签名口径 #104/#119） | ✅ 已完成（2026-09-14 v0.26.0 发版 + DT50 真机换签名重装验收通过（图标 / 版本 / 冒烟打印，用户目视确认；设备号按预期变更）），[#31](https://github.com/marci-labs/LabelFrame/issues/31)） |
| 60 | 安装引导专项 4/8——引导程序骨架：五步问卷与拓扑预设→组件集合（WiX Burn 形态 #122；dry-run 契约；装配缺陷返修 #81） | ✅ 已完成（2026-09-14 桌面自动化五步走查通过（组件集合正确 + Plan 预览一致 + dry-run 零系统改动实机复核），用户委托自动结项，[#53](https://github.com/marci-labs/LabelFrame/issues/53)） |
| 62 | 安装引导专项 6/8——安装编排：Burn Apply 执行链启用（运行时前置补装 + MSI 链 + 组件落位，#124；AspNetCore 前置两腿补齐 #128/#129） | ✅ 已完成（2026-09-15 干净 Win10 VM 三形态全流程验收通过：单机一体 + 服务端 / 客户端分离双预设组件集合正确 + 检测拦截与链内 AspNetCore 双实证；两轮返修后达成（PR #87 / #90），用户委托自动结项，[#55](https://github.com/marci-labs/LabelFrame/issues/55)） |
| 61 | 安装引导专项 5/8——下载引擎：多源回退、sha256 校验、重试续传、本地缓存（#125） | ✅ 已完成（2026-09-15 v0.27.0 公网真实走查通过：8 资产公网直下 sha256 与 manifest 及 GitHub digest 三方一致 + latest.json 指针语义正确 + 引导引擎公网全链拉取实证（升级走查 Burn 日志）；引导 EXE 随发版接线维持 #122 后置决策），用户委托自动结项，[#54](https://github.com/marci-labs/LabelFrame/issues/54)） |
| 安装引导专项 | 迭代 57~64（#50~#57）8 迭代汇总：设计契约（#114~#118）→ CI 生成 manifest / latest.json（#120）→ PDA 签名稳定化 + 下载中心扫码（#119）→ 引导骨架（#114→#122 Burn Pivot）→ 下载多源回退（#125）→ Apply 执行链（#124）→ 官方插件外置化（#123）→ 升级路径收尾（#126：BA 升级清单 + 客户端检查更新）；真机 / VM / 发版依赖项转 `待验收` | ✅ 专项交付完成（2026-09-14；逐迭代状态见上表与各自 Issue；回看串讲见 DESIGN §6.7，[#57](https://github.com/marci-labs/LabelFrame/issues/57) 收尾） |
| 67 | 流程治理——release.yml 步骤序修复：Client MSI 附带 Zebra 官方插件包 + 发版链 fail-closed 断言（决策 #131；v0.27.0 发版 MSI 缺 plugin-packages\*.lfplugin，存量升级 zebra 自动安装不可能发生） | ✅ 已完成（2026-09-16 本地同构演练验收：AC-01 正向断言 File 表命中 + AC-02 缺包反向 exit 1 拦截后恢复重跑通过；v0.27.1 补丁发版由用户推 tag 触发后走查复核，[#97](https://github.com/marci-labs/LabelFrame/issues/97)） |
| 63 | 安装引导专项 7/8——品牌传输插件外置化与安装期按需组装（zebra \`.lfplugin\` 官方插件体系 #123 / manifest 收录与品牌映射 / 存量升级自动安装） | ✅ 已完成（2026-09-16 v0.27.1 存量升级走查通过：升级后 WinHost 首启自动安装外置包（host.log「存量升级迁移」链 + \`loaded=true\`/\`isExternal=true\`）+ Release 三件套核验（.lfplugin 附件 / manifest 条目 / Client MSI 附带包）；两轮返修后达成（PR #86 生命周期 / PR #98 MSI 附带包），用户委托自动结项，[#56](https://github.com/marci-labs/LabelFrame/issues/56)） |
| 64 | 安装引导专项 8/8——升级路径：引导程序升级模式（升级评估与可升级清单 #126）+ 客户端检查更新提示 + 专项记账收尾 | ✅ 已完成（2026-09-16 v0.27.1 真实覆盖升级走查双路径通过（0.26.0 直升 + 坏 0.27.0 修复升级；配置保留决策 #48 sha256 逐字节一致 + WinHost 启动正常——依赖降级缺陷消除）；依赖固化返修（PR #96 / 决策 #130）后达成，用户委托自动结项，[#57](https://github.com/marci-labs/LabelFrame/issues/57)） |
| 发布 | v0.27.0 安装引导专项（迭代 60-64）汇总发布（引导下载多源回退 / Apply 执行链与运行时前置 / zebra 传输外置化官方插件体系（.lfplugin 首发随 Release 分发）/ 升级路径与客户端检查更新；v0.26.0 标题「迭代 64」列名勘误——实随本版合入） | ✅ 已完成（2026-09-15，tag v0.27.0） |
| 发布 | v0.27.1 补丁发布（迭代 64 返修 #96：覆盖升级依赖降版——发布工件版本固化 + 降版回归断言 #130；迭代 67 #98：release.yml 步骤序——Client MSI 附带插件包 fail-closed 断言 #131；发版后走查复核交回验收 #56 / #57） | ✅ 已完成（2026-09-16，tag v0.27.1） |
| 68 | 流程治理——引导 EXE 随 Release 发布（release.yml bundle job 接线 + manifest 分阶段生成与跨 job 一致性断言 #132）+ 安装引导文档补章（DEPLOY §2 推荐入口 / README 入口定位） | ✅ 已完成（2026-09-17 本地同构演练通过：v0.27.1 产物在场 manifest 分阶段生成 + bundle 全链缓存命中 + 三方哈希一致 + `-Sign` 签名路径 + fail-closed 反向用例；演练顺带抓修 PowerShell 变量名大小写覆盖参数缺陷；真实发版走查（AC-02）与干净 Windows 环境七页向导（AC-03）转 `待验收`，[#101](https://github.com/marci-labs/LabelFrame/issues/101)）→ ✅ 验收结项（2026-09-20 v0.28.0 发版走查 + Hyper-V 干净 Windows 真机陪验通过：Release 附件 LabelFrame-Bootstrapper-0.28.0.exe 双路下载 SHA256 一致（0AD09775EFF27F0EFB0EC40A90DFA27312E93F167920F8CF5C146E859E0412E3）、七页向导全流程 + 装后核验全绿（LabelFrameServer 服务 Running / healthz 200 / 数据目录三库齐 / web-ui + zebra 插件落位带 .labelframe-bundle-placement 凭据）；未签名偏差（NotSigned）转 #169 跟进；用户委托自动结项） |
| 69 | 安装卸载收尾对称化——Bundle 卸载清理插件落位目录（来源 = #55 AC-01 干净 VM 重验观察项：zebra 落位目录残留约 10MB / 29 DLL，卸载重装不选 Zebra 品牌仍可用；卸载语义契约先行入 DESIGN §6.8「卸载清理」+ §6.9 链表扩九包 + 决策 #133——清理边界 = 只清 Bundle 落位目录、用户手动第三方 DLL 永不动；实现载体 = 成对清理包：落位包 `WebUiPlacement` / `ZebraPluginPlacement` 维持 Permanent 零改动，链内新增 `WebUiPlacementCleanup` / `ZebraPluginPlacementCleanup`（install 空操作登记 + UninstallArguments `-clean`）+ 落位凭据 `.labelframe-bundle-placement`（内容 = Bundle 版本，只有 Bundle 落位目录携带）+ PayloadTool `-clean` 模式（幂等 / 失败留痕不阻断卸载主链）；清理时机覆盖卸载 / Modify 改选 / 安装失败回滚） | ✅ 已完成（2026-09-17 PR #103 dotnet build / test 全绿（新增 `PayloadCleanupTests` 8 项：清理 / 重装不选品牌 / 不误伤 / 凭据语义）+ 本地同构走查脚本 24 断言全过，CI 三项必需检查全绿 squash 合入 58e96e8（AC-03 / AC-04 / AC-05 过合并阻断点）；真机走查（AC-01 / AC-02）转 `待验收`，[#93](https://github.com/marci-labs/LabelFrame/issues/93)）→ ✅ 验收结项（2026-09-20 Hyper-V 纯净 Windows 真机（LF-Accept-Win10）陪验通过：ARP 卸载后 web-ui / zebra 落位目录清理干净 + MSI 组件卸净（AC-01）、重装不选 Zebra 后 zebra 目录不存在 + 客户端品牌选择器「无可用品牌」（AC-02）、手动第三方 DLL 保留真机复核（AC-03）；覆盖升级链观察项（非 AC）转 #171 `待验收`（恢复条件 = v0.29.0 发版后走查）；用户委托自动结项（2026-09-11 分工协议），证据 `artifacts/accept-93/`（gitignore）） |
| 71 | Linux 服务端一键安装——install.sh（manifest 校验 + systemd + 离线布局 + 归档默认 self-contained）与 compose 随发版分发（Linux 部署契约 #134） | ✅ 已完成（2026-09-17 本地 Docker 容器自证：离线零外网全流程 + 篡改 fail-closed + 重跑幂等 + 在线 404 回退 + FDD runtime 双向 + 真实 v0.27.1 manifest 解析 + AC-04 compose 步骤同构演练；随迭代修复 deploy-server-ubuntu.sh 字面换行符缺陷；真机 Ubuntu 走查与下次 `v*` 发版附件实证转 `待验收`，[#91](https://github.com/marci-labs/LabelFrame/issues/91)）→ ✅ 验收结项（2026-09-18 v0.28.0 发版实证：公网通道 systemd 容器内真实 GitHub 下载链 sha256 校验 + 版本一致 + 幂等重跑保留配置；Release 附件 compose.yml/.env 在场且 docker compose config 通过；linux 归档 self-contained 抽查确认；用户发布确认） |
| 70 | 离线布局安装——预下载组件目录（VS layout 式：`make-offline-layout.ps1` + 引导 `--layout` 双形态）与本地源无网首装（隐式优先源目录 + sha256 本地源同样强制，源解析顺序决策 #135） | ✅ 已完成（2026-09-17 断网 VM 实机取证通过：防火墙阻断外网形态下等效干净机七页向导全流程 + 邻接清单默认 + 四组件「本地源命中」+ 全程零 download-from + 防火墙日志零外网请求（DROP=0 / 公网目标=0）+ 篡改拦截 fail-closed 实机复核 + 装后核验全绿（53961/53960=200、zebra 插件落位 loaded/isExternal）；官方 Release 生成走查随下次 `v*` 发版复验，[#89](https://github.com/marci-labs/LabelFrame/issues/89)） |
| 72 | 模拟打印出图目录保留清理（Log 出图 print 目录超期自动删除；按天保留默认 31 天 / 可配置 / 可关闭 #136，口径对齐 #108） | ✅ 已完成（2026-09-17 本地 build / test 全绿，新增四类用例 6 项实证：超期删除含 PNG 与摘要留痕 / 保留期内不删 / ≤0 关闭 / 占用失败降级留痕且打印出图主链路不受影响；跨月滚动删除行为随部署观察，[#107](https://github.com/marci-labs/LabelFrame/issues/107)） |
| 73 | 界面文案用户化——开发者向文案修正（模式名 / 路径 / 扩展名 / 内部机制术语用户化或删除，决策 #138）与连接配置低频收纳（设置页「连接方式」默认折叠为摘要一行） | ✅ 已完成（2026-09-17 本地 lint / 304 项前端测试 / 双构建全绿，行为零变化；2026-09-17 沙箱界面走查客观面验收通过（折叠一步展开 / 切换-测试-保存回归 / 双端 12 页术语扫描 0 命中），文案观感留 #114 评审，用户委托自动结项，[#108](https://github.com/marci-labs/LabelFrame/issues/108)） |
| 74 | 模拟打印作业数据留痕——Log 模式提交记录模板名与传入数据集合（数据集合去重 + 重复张数；单条 ≈2KB 截断并标注完整长度 #139） | ✅ 已完成（2026-09-17 本地 build / test 全绿，新增 14 项用例实证：单张字段键值 / 批量相同去重 / 相同 + 不同混合 / 键序无关去重 / 超限截断 + 完整长度标注 / 边界不截断 / 空数据占位 / 换行转义 / 未命名回退 / 幂等重放不重复留痕 / 非 Log 模式零留痕（AC-04）；2026-09-17 沙箱实机验收通过（AC-01 实机口径：当日 host 日志单张 / 去重（重复 N 张）/ 截断恰 2048 留痕行 + 单测 11/11 复核），用户委托自动结项，[#109](https://github.com/marci-labs/LabelFrame/issues/109)） |
| 75 | 「PDA 日志 / 设备日志」页面下线——回传链路不存在前的界面收敛（双形态导航入口 + 路由移除、组件与测试删除；`/api/logs` 端点与 logs.db 后端保留，决策 #140） | ✅ 已完成（2026-09-17 本地 lint 0 错误 / 300 项前端测试 / 双构建全绿，后端零改动；2026-09-17 沙箱走查验收通过（双端导航无日志项 + 直连路由无残留 + `GET /api/logs` 双端 200 保留），用户委托自动结项，[#112](https://github.com/marci-labs/LabelFrame/issues/112)） |
| 76 | 插件命令打印契约设计——文档编译能力接口与打印方式参数（纯文档，决策 #137：提交时编译并持久化 / 连接级打印方式 / 编译显式失败不回退；实现拆分建议见 DESIGN §5.4.8） | ✅ 已完成（2026-09-17 契约与拆分建议成文，AC 自证；实现迭代按 §5.4.8 立项，[#113](https://github.com/marci-labs/LabelFrame/issues/113)） |
| 77 | 插件命令打印实现 1/4——宿主链路落码（Core `ILabelCommandCompiler` 契约 + descriptor 能力位 / DTO 透出 + `printMode` 保存校验与提交分派 + `LabelJobItem.Zpl` 语义泛化 + `LF_ENC_002` / `LF_ENC_003` + fake 编译器单测矩阵） | ✅ 已完成（2026-09-17 fake 编译器单测矩阵 26 项全绿 + 既有套件 798 项零回归（排除 Perf/Soak）；Zebra 编译器实现与真机效果验收归步 2 / 3（#120 / #121），[#119](https://github.com/marci-labs/LabelFrame/issues/119)） |
| 78 | 插件命令打印实现 2/4——Zebra 编译器·文本（`ZebraLabelCompiler` 整页自包含 + `^A` 字体 / 字高 DPI 换算 + 内置字体度量 `ZebraTextWidthMeasurer` 接 `ITextWidthMeasurer` 锚定尽力近似 + 中文 `LF_ENC_002` 显式拒绝 + `printMode` 参数声明 + 前端「无预览，效果以真机为准」提示） | ✅ 已完成（2026-09-17 本地 build / test 全绿，新增 WinHost 30 项（编译器与度量器 27 + 真实插件宿主集成 3）+ 前端 7 项；AC-04 浏览器自动化走查截图附 Issue 回评；真机文本对比（AC-06）转待验收（恢复条件 = Zebra 真机可得），[#120](https://github.com/marci-labs/LabelFrame/issues/120)）→ ✅ 验收结项（2026-09-18 Zebra 真机陪验通过：图片 / 原生两轮并排（40×30 + 70×50）文本可读、加粗近似可见、溢出符合裁剪口径、锚定偏移已记录（根因在图片模式基准渲染器，建议另行立项）、中文文本图片正常 / 原生 LF_ENC_002 拒绝实证；用户确认） |
| 79 | 插件命令打印实现 3/4——Zebra 编译器·条码与二维码（Code 128 `^BC` / `^BY` 编译：模块宽与高度 DPI 换算 + `displayValue` interpretation 映射 + 中文 `LF_ENC_001` 既有语义零回归；QR `^BQ` 编译：ECC 双处映射 + `qrMargin` 静区映射 + 放大倍数按尺寸与 DPI 换算（版本容量估算）+ UTF-8 中文经 `^CI28`；混合模板整页自包含） | ✅ 已完成（2026-09-17 本地 build / test 全绿（850 项，排除 Perf/Soak），编译器新增 16 用例（理论展开 22 例）+ 宿主集成 2 例；真机扫码对比（AC-04）转待验收（恢复条件 = Zebra 真机 + 扫码枪可得），[#121](https://github.com/marci-labs/LabelFrame/issues/121)）→ ✅ 验收结项（2026-09-18 Zebra 真机 + PDA 扫码枪陪验通过：两模式 Code128 / QR（ASCII）均扫码命中且人眼可读；QR 中文真机验证完成——该打印机族不支持（图片扫码不解、原生过滤中文），用户拍板产品口径 QR 仅 ASCII、中文只走文本框；用户确认） |
| 80 | 服务端地址切换「保存即生效」落地与连接状态一致性（评审 #114 C-2 / B-9 / C-3：`ServerRoutingCoordinator` 路由热切换——保存后重建 poller / worker 注册新服务端并清理旧连接，决策 #141；客户端「三名义」——本机打印服务 / 服务端地址 / 已加入服务端各义一名；数据与打印加入状态随探测周期刷新） | ✅ 已完成（2026-09-17 本地 dotnet build / test 全绿（含热切换集成测试双 stub 服务端模拟 AC-01）+ pnpm lint 0 错误 / 311 项前端测试 / 双构建；运行中切换按实现行为口径以集成测试断言，[#128](https://github.com/marci-labs/LabelFrame/issues/128)）→ ✅ 验收结项（2026-09-18 沙箱双实例陪验走查通过：保存后 128ms 注册新服务端零重启、停 / 启服务端约 13s / 16s 双向自动刷新、三名义同屏一致；用户确认，证据 `artifacts/accept-128-129-130/`） |
| 81 | 「图片预览」名副其实——页内预览弹层与下载入口分离（评审 #114 C-1：数据与打印页点击「图片预览」页内弹层呈现当前字段值渲染图（复用 render-image 实时取图，不建作业不自动下载）+ 弹层内显式「下载」按钮，预览与下载彻底分离，决策 #142；模拟出图批量 zip 链路与工作台缩略图预览现状保留） | ✅ 已完成（2026-09-17 本地 pnpm lint 0 错误 / 318 项前端测试 / 双构建全绿，后端零改动，[#129](https://github.com/marci-labs/LabelFrame/issues/129)）→ ✅ 验收结项（2026-09-18 沙箱双形态陪验走查通过：弹层即时反映当前字段值、弹层内下载显式分离、模拟出图行为零回归；用户确认，证据 `artifacts/accept-128-129-130/`） |
| 82 | 打印机连接文案用户化收尾（评审 #114 B-1 / A-1 / B-5 / B-2：Log 传输三处徽标「模拟打印」无「LOG」直出——改插件 Describe 覆盖插件路径、回退路径迭代 73 已就绪，displayText 契约字段不变；插件说明去「联调」+ 前端无参数不追加「（无参数）。」残句；TCP 选项列表「网口打印机（TCP 9100）」，决策 #143） | ✅ 已完成（2026-09-17 本地 dotnet build / test 全绿（856 项，排除 Perf/Soak）+ pnpm lint 0 错误 / 323 项前端测试 / 双构建；AC-01 / AC-02 以前端测试断言覆盖（沙箱浏览器走查为可选项），[#130](https://github.com/marci-labs/LabelFrame/issues/130)）→ ✅ 验收结项（2026-09-18 沙箱双形态陪验走查通过：三处「模拟打印」、说明文与 TCP 标签逐项目视确认；用户确认，证据 `artifacts/accept-128-129-130/`） |
| 83 | 模板字段显示名与设计器属性提示修正（评审 #114 B-3 / B-4 / A-2 后半：设计器字段绑定可选「显示名」+ 显示处一律 `displayName || key` 回退——数据与打印页表单标签 / 占位符、侧栏「打印字段」列表、测试默认值，契约 fields 既有 displayName 字段格式零变化、存量模板回退键名零迁移，决策 #144；术语「契约字段 / 键名称」→「打印字段 / 字段名」；属性面板字段填充提示移入字段填充分支（固定值模式不显示）） | ✅ 已完成（2026-09-17 本地 pnpm lint 0 错误 / 336 项前端测试 / 双构建全绿，后端零改动；AC-01～AC-04 以前端测试断言覆盖（含模板包往返不丢显示名），[#131](https://github.com/marci-labs/LabelFrame/issues/131)） |
| 84 | 打印页称谓与作业信息可读性（评审 #114 A-3 / B-6 / B-7：数据与打印页副标题「填写数据并打印 / Excel 批量打印」（决议 1，去「测试」自称）；作业进度与作业历史「目标设备」统一显示设备名——GET /api/devices 的 name 同源、无可解析名称回退设备 ID，与在线设备页 / 目标设备下拉一致；作业历史编号收敛（决议 2）——仅「作业编号」可见可一键复制完整编号、「请求编号」收进悬停提示，报障口径统一为作业编号，决策 #145） | ✅ 已完成（2026-09-17 本地 pnpm lint 0 错误 / 345 项前端测试 / 双构建全绿，后端零改动；AC-01～AC-03 以前端测试断言覆盖（双形态），[#132](https://github.com/marci-labs/LabelFrame/issues/132)） |
| 87 | 连接选项「Log（模拟打印）」去技术前缀（#130 走查观察项：`LogTransportPlugin.DisplayName`「Log（模拟打印）」→「模拟打印」，选项列表 / 选中态反馈同步；插件 Id `log` 存量零迁移） | ✅ 已完成（2026-09-18 PR #145 本地 dotnet build / test 856 项全绿（排除 Perf/Soak）+ pnpm lint 0 错误 / 346 项前端测试 / 双构建，CI 三项必需检查全绿；验收沙箱走查通过（AC-01 选项列表「模拟打印」无「Log」字样 + 选中态说明 / 摘要一致，AC-02 旧格式 connection.json（pluginId=log）启动连接加载保持 + 新名显示），用户委托自动结项（2026-09-11 分工协议），证据 `artifacts/accept-143/`（gitignore），[#143](https://github.com/marci-labs/LabelFrame/issues/143)） |
| — | 评审：界面可用性走查——用户视角初审 + 用户复核 + 排期（22 项三级标注；高/中优先立项迭代 80–85，低优先 6 项入待需求） | ✅ 已完成（2026-09-17 用户复核确认 17 / 补充 5 / 驳回 0，C-2 / C-1 实测复现；截图与沙箱存档 `outputs/review-114/`（gitignore），[#114](https://github.com/marci-labs/LabelFrame/issues/114)） |
| 86 | 设置页「服务端已连接」徽标空地址谎报修复（#128 走查观察项：serverUrl 为空时 serverApi base 落同源打到本机 WinHost 谎报已连接、同页安装包 / 插件区 404 同屏矛盾；初始化与机器级配置加载探测竞态双态。a 案 = 空地址不探测直接判「服务端未连接（单机模式可用）」+ 探测按配置就绪串行化与序号后发优先，决策 #147） | ✅ 已完成（2026-09-18 PR #153 本地 pnpm lint 0 错误 / 354 项前端测试（双模式）/ 双构建全绿，dotnet build / test（856 项，排除 Perf/Soak）零回归，后端零改动；验收沙箱走查通过（AC-01 空地址徽标「服务端未连接（单机模式可用）」+ 安装包 / 插件区单机提示、同屏无矛盾、零请求到默认地址 53961，AC-02 可达 / 不可达 / 空地址三段 UI 保存切换正确、空地址后 22s 零远端探测、三段稳态采样无竞态双态，30/30 断言），用户委托自动结项（2026-09-11 分工协议），证据 `artifacts/accept-142/`（gitignore），[#142](https://github.com/marci-labs/LabelFrame/issues/142)） |
| 89 | 图片模式锚定文本垂直坐标语义修复（迭代 78/79 真机陪验观察项：区域锚定 Center/Center 自动宽度文本下半穿出锚定盒底边 1.5–2 字高——`SkiaLabelRenderer` / `AndroidLabelRenderer` 把决策 A 兜底框（≥10mm）当锚定框参与框内居中致字形中心下沉；修复 = 锚定自动高度文本绘制框改用解析锚定框 `bounds.HeightMm`，非锚定兜底框与原生 Zebra 编译器零变化，决策 #146） | ✅ 已完成（2026-09-18 PR #154 dotnet build / test 860 项全绿（排除 Perf/Soak），CI 三项必需检查全绿；验收沙箱走查通过（AC-02 双锚定盒 60×8mm 版式渲染像素分析：墨迹重心偏差 +0.042 / +0.062mm（容差 ±1mm）、盒底以下零墨迹，probe 盒墨迹 [23.523, 24.649]mm 与 PR #154 修复后探针独立复现一致），用户委托自动结项（2026-09-11 分工协议），证据 `artifacts/accept-147/`（gitignore），[#147](https://github.com/marci-labs/LabelFrame/issues/147)） |
| 90 | Zebra 插件说明「原生指令模式下 QR 不支持中文」（#121 真机实证：原生 `^CI28` 下中文被打印机静默过滤——Description / printMode Hint 补「原生指令模式下二维码（QR）数据不支持中文（仅 ASCII），中文请用文本框（图片模式）」口径，限定原生指令模式不外溢图片模式；编译行为保持现状未升级 fail-closed，用户拍板 a 案） | ✅ 已完成（2026-09-18 PR #155 dotnet build / test 861 项全绿（排除 Perf/Soak，净增文案契约断言 1 项），CI 三项必需检查全绿；验收沙箱走查通过（AC-01 设置页连接方式编辑区选中 Zebra：Description 追加句可见 + printMode 提示同口径限定原生模式（title 逐字匹配）+ 切原生指令警示徽标即时反馈；走查对象为最新 master 重建的 0.27.1 插件包），用户委托自动结项（2026-09-11 分工协议），证据 `artifacts/accept-148/`（gitignore），[#148](https://github.com/marci-labs/LabelFrame/issues/148)） |
| 91 | 前端请求层健壮性（前端评审 F-01 / F-09 / F-12 / F-13：`makeRequest` / `makeFetchBlob` 统一挂超时——普通 30s + 出图 / 上传类 120s 分档（用户拍板 a 案，决策 #148），超时归一化中文 `ApiError('TIMEOUT')` 走既有错误通道；`exportTemplate` 改 `makeFetchBlob` 同构——失败解析后端 ErrorView 呈现真实原因、Content-Disposition 解析收敛一处；DataPrint 模板详情 effect 加 `cancelled` 竞态守卫（与预览弹层同一标准）；Designer 加载 effect 补 `serverMode === 'unknown'` 守卫与 Workbench 对齐（单次闩锁防模式翻转重拉）） | ✅ 已完成（2026-09-18 PR #159 pnpm lint 0 错误 / 双模式各 367 项前端测试（新增 13 项）/ 双构建全绿，后端零改动 dotnet build 0 警告 0 错误 / test 635 项（排除 Perf/Soak）零回归，CI 三项必需检查全绿；验收沙箱走查通过（AC-01 Playwright 路由拦截「接了不回」挂起保存 POST /api/templates：30495ms 后按钮恢复「保存模板」+ 状态栏「请求超时（服务端超过 30 秒未响应），已取消本次请求，请检查服务状态后重试。」与实现口径逐字一致、12s 中途复核仍在「保存中…」排除立即失败；AC-02 导出已删（陈旧卡片）模板：后端 404 LF_SRV_006「模板不存在:accept-149-100x60。」逐字透出、非固定「导出失败（HTTP 404）」；AC-03 / AC-04 复跑 client / DataPrint / Designer 三测试文件 61 项确定性断言全绿），用户委托自动结项（2026-09-11 分工协议），证据 `artifacts/accept-149/`（gitignore），[#149](https://github.com/marci-labs/LabelFrame/issues/149)） |
| 92 | 设计器未保存离开保护与页级测试补齐（前端评审 F-02 / F-03：dirty（决议 a：历史栈有任一已提交更改即 dirty，`undoCount > 0`，决策 #149）时 Shell 导航切 tab 与设计器返回按钮（含加载失败返回）均弹三选 Modal「保存并离开 / 放弃更改 / 继续编辑」——切 tab 统一经 `switchTab` 入口 + 设计器 `registerLeaveGuard` 注册守卫（挂载注册 / 卸载注销），挂起离开动作 `pendingLeaveRef` 在各终止路径（继续编辑 / 保存失败 / 覆盖取消 / 名称缺失）就地作废防陈旧动作误触发；「保存并离开」走既有保存链路（空名校验 / 同名覆盖确认），成功后执行挂起离开、失败停留显示错误不丢编辑；Esc / 遮罩 / 右上角关闭 = 继续编辑（默认安全侧）；无编辑（undoCount = 0）直接离开零打扰，普通保存成功回工作台既有行为零变化；补 `Designer.test.tsx` 页级测试 10 项（mock `biz` 单机范式，画布维持决策 #82③ 断言边界）） | ✅ 已完成（2026-09-18 PR #160 pnpm lint 0 错误 / 双模式各 377 项前端测试（新增 10 项）/ 双构建全绿，纯前端改动 dotnet build 0 警告 0 错误 / test 861 项（排除 Perf/Soak）零回归，CI 三项必需检查全绿；验收沙箱双形态走查通过（AC-01 dirty 三选三路径 + 返回按钮：Modal 标题「未保存的更改」与三按钮逐字命中——「继续编辑」留设计器编辑不丢 / 「放弃更改」直离且 Server 元素数未变 / 「保存并离开」成功后完成挂起切换且 Server 落库；AC-02 无编辑三场景（加载未动切 tab / 返回按钮 / 新建未动）直接离开无弹层；AC-03 路由拦截 POST /api/templates 注入 500 `LF_TPL_SAVE_FAILED`：停留设计器 + 状态栏逐字显示中文原因 + 编辑不丢，解除拦截后普通保存成功回工作台 Server 落库（挂起作废后重试可用）；AC-04 / AC-05 已证项走查冒烟零 pageerror，31/31 断言 PASS），用户委托自动结项（2026-09-11 分工协议），证据 `artifacts/accept-150/`（gitignore），[#150](https://github.com/marci-labs/LabelFrame/issues/150)） |
| 93 | 前端交互一致性收尾（前端评审 F-04 / F-06 / F-07 / F-11 / F-16 / F-17 / F-20 / F-21：`DownloadCenter` / `PluginPackages` / `Settings` 的 `window.confirm` 全部改自研 `Modal` 确认（取消 + 危险色确认，复用工作台删除确认模式；`Designer` 剪贴板完全禁用时 `window.prompt` 终极兜底保留）；`styles.css` 增 `.banner` / `.banner.error` / `.banner.notice` 通用类，7 处错误 + 3 处通知横幅统一引用、内联复制清零；`Devices` 初始 `null` +「正在加载设备列表…」加载态（决议 a 案，消除首进闪现「暂无设备」）；工作台页头「刷新」按钮 + 失败横幅「重试」入口；DataPrint 列映射表头「字段键」→「模板字段」；lint 6 warnings 逐一修复 / 豁免清零；死代码 `TransportQuickSwitch` / `findElement` / `elementTypeName` 删除；`vitest.config.ts` 残留 `server.port` / `build.outDir` 段删除） | ✅ 已完成（2026-09-18 PR #162 pnpm lint 0 警告 0 错误 / 双模式各 378 项前端测试（新增 1 项）零回归 / 双构建全绿，纯前端改动 dotnet build 0 警告 0 错误 / test 861 项（排除 Perf/Soak）零回归，CI 三项必需检查全绿；验收沙箱双形态走查通过（AC-02 抽查 4 处横幅 / 3 页：工作台 / 设计器 / 在线设备错误横幅 + 在线设备通知横幅（真实操作「设为默认」），路由拦截 500 制造失败——四处计算样式逐项一致（`flex` / `6px 16px` / `12px`，danger-soft / accent-soft 底色 + danger / accent 字色）与 `.banner` 通用类定义吻合、几何与内容盒对齐（60/1540）无横向溢出；AC-03 拦截 `GET /api/devices` 挂起首进显示「正在加载设备列表…」断言无「暂无设备」闪现、放行走真实响应渲染在线设备表；AC-04 页头刷新按钮在场可用 + 拦截列表 GET 500 出失败横幅（注入中文原因逐字呈现）与「重试」按钮、解除拦截重试成功恢复模板卡片；AC-01 顺带抽查工作台删除确认自研 Modal——「取消」+ 危险色「确认删除」、`page.on('dialog')` 零触发（非原生 confirm）、440px 弹窗双轴居中偏差 0；走查 17/17 断言 + 几何量测 3/3 PASS、双页零 pageerror），用户委托自动结项（2026-09-11 分工协议），证据 `artifacts/accept-151/`（gitignore），[#151](https://github.com/marci-labs/LabelFrame/issues/151)） |
| 发布 | v0.28.0 迭代 68-84、86-87、89-93 汇总发布（安装与分发链补全：引导 EXE 首次随发版构建发布 / Linux 一键安装 install.sh + compose 分发附件 / linux 归档默认 self-contained / 离线布局安装与卸载对称化；插件命令打印落地 3/4：契约 + 宿主链路 + Zebra 文本・条码与二维码编译；界面文案用户化与前端健壮性系列收尾；发版后走查复核联动待验收 #91 / #93 / #101） | ✅ 已完成（2026-09-18，tag v0.28.0） |
| 94 | 流程治理——release.yml 前端产物自检特征过时修复与 v0.28.0 恢复出库（client 特征「PDA 日志」随迭代 75 页面下线消失致发版链确定性失败（run 35326712703）+ 反标记「本机未注册到服务端」迭代 80 改名后空转——两处同改「本机打印服务」（本地双构建取证 client 在场 / server 不在场），server 特征「客户端下载」维持，决策 #150；出库走 workflow_dispatch version=0.28.0，不删不重推 tag） | ✅ 已完成（2026-09-18 本地双构建取证 + YAML 自检；dispatch 出库与 Release 附件核验证据回写 #166（AC-02~05），[#166](https://github.com/marci-labs/LabelFrame/issues/166)） |
| — | 缺陷排查：v0.28.0 出库链签名缺口定位（#169 只读排查，口径 A）——根因＝签名 Secrets（`MSI_SIGN_CERT_BASE64` / `MSI_SIGN_PASSWORD`）未配置于仓库＋release.yml「Secret 缺失即静默跳过」、唯一断言只查 EXE 在场不查签名状态（无 fail-closed）；附带发现 package job 证书步骤自引入起缺 env 映射（即便补配 Secret 双 MSI 在 CI 也不会签）；「v0.28.0 清单附件过期」前提证伪（附件为补跑现生成 0.28.0 内容，webview2 哈希跨版本相同系 evergreen 直链内容偶合）；修复建议已回写 Issue，转流程治理立项 | ✅ 已完成（2026-09-20 四方向结论＋行号级证据＋修复建议回写 Issue；只读排查零文件变更，[#169](https://github.com/marci-labs/LabelFrame/issues/169)） |
| 85 | 模板直达打印与作业明细查看（评审 #114 C-4 / C-5：工作台卡片操作区新增「打印」按钮——写打印草稿与手动选择同一入口后切「数据与打印」页、模板已预选，双击卡片仍进设计器；作业历史行展开——本机直连显示逐张明细（状态徽标 / 失败原因 errorMessage 优先回退 errorCode，WinHost 列表端点本就返回逐张 items、后端零改动），服务端形态显示汇总＋决议 2 如实占位；数据与打印进度区纯文字指引改可点击跳转；出图查看入口评估不做——需 WinHost 文件服务端点属打印 API 契约扩展，边界与未来方向记 DESIGN 决策 #152） | ✅ 已完成（2026-09-20 PR #179 pnpm lint 0 警告 0 错误 / 双模式各 387 项前端测试（新增 9 项）/ 双构建全绿，后端零改动 dotnet test（排除 Perf/Soak）871 项零回归，CI 三项必需检查全绿 squash 合并 eaa3fda；AC-01~03 界面沙箱走查转 `待验收`，[#133](https://github.com/marci-labs/LabelFrame/issues/133)）→ ✅ 验收结项（2026-09-20 双沙箱独立端口（53980-53982）浏览器自动化走查 23/23 断言通过（Log 模拟打印、53960/真机未触碰；AC-04 已随 PR #179 合并实证）；失败张为沙箱库级注入已如实披露（形态与 JobPrintWorker 写入口径一致）；出图查看入口不做属 DESIGN 决策 #152 既定边界；用户委托自动结项（2026-09-11 分工协议），证据见 Issue 走查总评论） |
| 95 | 安装引导程序体验收敛——就绪页清单自动加载（进入向导即自动获取，失败展开手动来源区）+ 两层问卷（基础流 = 打印机品牌→服务端地址→简版确认；「高级选项」= 本机角色（仅客户端 / 服务端±同机客户端）+ 管理界面开关 + 组件来源 / 清单覆写 / 离线说明折叠区）+ Docker / Linux 形态从向导移除（文档承接指引）+ 服务端地址经 MSI property 透传（装完即配好客户端）+ 全向导文案按「默认零技术细节」精简（manifest schema 零变化，决策 #151） | ✅ 已完成（2026-09-20 PR #178 契约测试与 E2E 走查脚本适配后本地 dotnet build / test 全绿（排除 Perf/Soak），CI 三项必需检查全绿 squash 合入 master `3a5fd83`；AC-01～09 全 ✅——AC-09 随 PR 合并实证、AC-01～08 于 2026-09-20 Hyper-V 干净 VM（LF-Accept-Win10）两任真机走查实证通过（AC-03 settings.json 地址透传、AC-06 hosts 模拟无网回退、AC-07 回归脚本 20/20——2 条横幅断言红属脚本适配滞后转 #181 非产品缺陷；AC-08 主观抽查点 2 条列证供用户事后异议），用户委托自动结项（2026-09-11 分工协议），证据 `artifacts/accept-175/`（00–24 截图 + e2e 日志，见 Issue 总评论），[#175](https://github.com/marci-labs/LabelFrame/issues/175)） |
| — | 缺陷修复：引导器 WebView2 evergreen 载荷钉死哈希随微软轮换失效（v0.28.0 全新装机校验失败）——验签治本（DESIGN 决策 #153）：evergreen 载荷改微软 Authenticode 发布者验签（链完整 + 签名者 CN=Microsoft Corporation 双条件 fail-closed），载体改内嵌 PayloadTool 于包执行期下载验签安装；配套已装（Present）机器跳过 acquire（runtime 包 `Cache` 改 `remove`，机制依据 wixtoolset/issues#7021）；布局链路同策略（OfflineLayoutBuilder / make-offline-layout / build-bundle 构建期不再钉哈希下载） | ✅ 已完成（2026-09-21 PR #177 三次 rebase 后 CI 三项必需检查全绿（run 35553586518）squash 合入 master `98a0390`；沙箱走查 9/9（真实 fwlink 验签正例 + 篡改 / 未签名 / 发布者不符三反例拒绝 + Present 零请求 / 缺失正常获取对照）＋ Bootstrapper 单测 265/265；AC-1 契约用户评审（补审）与 AC-4 真机全新装机走查转 `待验收`，[#173](https://github.com/marci-labs/LabelFrame/issues/173)）→ ✅ 验收结项（2026-09-21 Hyper-V LF-Accept-Win10 纯净基线真机两向走查通过：首装（WebView2 已在场＝9-20 原始失败同态）WebView2Runtime Present→execute None **零获取零校验**、.NET 双 runtime 厂商直链验签安装 0x0、ClientMsi 本地源安装 0x0、总退出 0x0；幂等二跑全包 Present→None、零 Acquiring、源零请求、2 秒退出 0x0；「缺失→fwlink 下载→Authenticode 验签→静默安装」路径由沙箱走查（真实 fwlink 验签通过 + 签名者 Microsoft Corporation + 三反例退出码 40 拒绝）与单测矩阵覆盖；AC-1 决策 #153 契约用户补审通过（2026-09-21 会话确认）；用户委托自动结项（2026-09-11 分工协议）） |
| 98 | 打印任务终态回调（webhook）——路由模式 `POST /api/jobs` 提交可选 `callbackUrl`（scheme 白名单 http/https 提交即拒，4xx 中文错误不入队），作业终态（Completed / Failed / Expired，含失联回收）由 Server 异步至少一次投递：`job_callbacks` 投递表＋后台扫描（指数退避 30s/1m/2m/4m 至多 5 次超限死信、状态持久化重启续投），三处终态转移点统一出口幂等登记＋登记自愈；作业视图暴露投递状态与末次错误；投递 10s 超时不跟随重定向响应读弃；直连模式（WinHost / AndroidHost）接受字段但忽略；HMAC 签名记 DESIGN 未决问题（决策 #154） | ✅ 已完成（2026-09-21 PR #191 CI 三项必需检查全绿 squash 合入 master `a153cd5`；DESIGN 决策 #154 先文档后代码（AC-01）；本地 dotnet build 0 警告 0 错误 / test 923 项全绿（排除 Perf/Soak，净增 23 项：成功 / 退避 / 死信 / 重启恢复 / 非法 scheme 拒绝 / 直连忽略 / 真实套接字投递）；AC-01~08 全部 A 类沙箱自证通过、无真人验收项，[#190](https://github.com/marci-labs/LabelFrame/issues/190)） |
| — | 测试债修复：升级走查脚本两条横幅断言锚点对齐 #151 现行文案（#181；`*不会下载*` 通配抢首命中致 S3 断言错位 → 横幅抓取收敛 UpgradePresentation.Summarize 三态锚点集 + S2 锚点改「检测到可用更新：服务端 <旧> → <新>」含版本号，与同场景 Burn 日志断言同口径） | ✅ 已完成（2026-09-21 PR #189 修复合入 `89bb203`（本地全量走查 18/18 + CI 三项必需检查全绿），欠账「全新安装臂未实际命中」于 2026-09-22 在 LF-Accept-Win10 纯净基线真机复跑补齐——S1 横幅实测「未检测到本机已安装的 LabelFrame 组件，将执行全新安装。」、三态锚点三场景各命中一次零错位，18/18 断言 exit=0，证据回写 [#181](https://github.com/marci-labs/LabelFrame/issues/181)） |
| — | 测试债修复：CI「构建与测试」偶发红治理——前端测试时序确定性改造（实证 5 处：JobHistory 列表渲染 / 轮询退避计数错位、DataPrint 切 tab、Settings 三分组 / TCP 参数表单——挂载等待 MOUNT_WAIT 8000ms、有界落定循环、`configure({ asyncUtilTimeout: 8000 })`；同类挂载用例整体治理：Devices 轮询 / Workbench.preview / App / Designer 等统一；vitest 单测超时 5s→20s）＋ WinHost FailureLog 超时根因定位（结论：**测试债非产品债**——产品失败路径无锁无固定睡眠，原固定墙钟预算被 CI 并行负载击穿；改截止时间轮询 30s 预算＋总 CTS 60s 解耦）；被测产品代码零改动（用户拍板 B 案整体治理） | ✅ 已完成（2026-09-22 PR #195 CI 三项必需检查全绿（run 35682001914）squash 合入 master `84c9f0c`；本地双模式各 387/387 全绿、治理 10 文件（175 项）双模式各连续 3 轮全绿、dotnet 923/923 零回归、FailureLog 连续 3 轮亚秒；AC-1~3 全 A 类沙箱自证通过，[#183](https://github.com/marci-labs/LabelFrame/issues/183)） |
| 发布 | v0.29.0 汇总发布（安装引导体验：迭代 95 两层问卷与清单自动加载＋#173 WebView2 Authenticode 验签治本（旧版引导器全新装机随微软轮换必败的缺陷修复）；使用体验：迭代 85 模板直达打印与作业明细；外部集成：迭代 98 终态回调 webhook；流程与测试债：迭代 94 / #183 CI 偶发失败治理 / #176 / #181；发版后走查复核联动待验收 #171） | ✅ 已完成（2026-09-22，tag v0.29.0；首跑失败于签名链缺陷转 #199，修复后 workflow_dispatch 恢复出库） |
| — | 流程治理：release.yml 签名链缺陷修复（#199；v0.29.0 首跑实证——bundle job 构建步骤**数组** splat 按位置绑定报「位置参数不能接受 artifacts/cert/labelframe.pfx」阻断出库；MSI job 证书步骤缺 env 映射致证书从不落盘、双 MSI 静默不签名（#169 定位遗留）＋两打包步骤缺密码映射——三处改哈希表 splat＋补两处 env，Secrets 未配置仍优雅跳过；dispatch 首跑再实证第四层——断言脚本 WindowsInstaller COM 只读句柄 RCW 未释放，signtool 签名写 MSI 被拒（file being used），补显式 ReleaseComObject + GC；出库以 workflow_dispatch version=0.29.0 恢复，tag 不删不重推（迭代 94 先例）） | ✅ 已完成（2026-09-22 修复合入；哈希表 splat 语义实证 + YAML 解析通过；dispatch 恢复出库与 Release 附件 / 签名态 / ghcr 镜像核验证据随 #199 回写，[#199](https://github.com/marci-labs/LabelFrame/issues/199)） |
| 99 | 流程治理——nightly-perf 治理：Perf 步骤拆分独立失败语义与阈值分环境口径（9-14 起连续失败排查定性三层：多行 pwsh 块退出码只取末条命令掩盖 Server 断言失败（8-31 / 9-7 两轮「成功」日志实有 FAIL）＋阈值按开发机标定而 GitHub 共享宿主 p50 轮间抖动 10-300 倍无判别力（同代码本机复跑全达标排除代码回归）＋9-14 起 runner 镜像更新后 WinHost 单张 31ms 顶红；Perf 段拆「Server Perf」「WinHost 单张 Perf」独立步骤，`GITHUB_ACTIONS` 分档——本地严格口径保留、CI 改「无错误 + p95 分层」（Routing p50 转观测 / WinHost 单张 p50<100ms p99<1s，决策 #155）；actions checkout@v7 / setup-dotnet@v6 清 Node 20 弃用警告；ci.yml / release.yml 的 actions 升级记待需求） | ✅ 已完成（2026-09-22 PR #200 CI 三项必需检查全绿 squash 合入 master `bd4d3bd`；本地 build 0 警 0 错 / test 923 项全绿（排除 Perf/Soak）+ Perf 双口径全绿（`GITHUB_ACTIONS=true` 下 device-20 p50=2051ms 通过实证分档分支生效）；合并后手动 dispatch nightly-perf 全步骤绿（AC-04 证据见 Issue），[#198](https://github.com/marci-labs/LabelFrame/issues/198)） |
| — | 缺陷返修：升级链获取阶段三重缺陷（#171 验收不通过回流——v0.28.0→v0.29.0 覆盖升级确定性失败真机实证）——① 落位/清理包常量 `CacheId` 跨版本共享缓存目录致升级哈希冲突 0x80091007 → 四包 `CacheId` 版本化；② 重跑会话附加容器 `WixAttachedContainer 0x80070002`（DESIGN 决策 #156 引擎级定论：包缓存 Bundle EXE 为设计上的截断桩，桩会话解容器走源搜索序而内嵌名 `bundle-attached.cab` 系构建中间名＝结构性死路，原始源可用性为分水岭）→ BA 容器获取事件注入 `SetLocalSource`（`WixBundleOriginalSource`，跨提权边界成立）；③ BA 多源回退重试热循环（真机 5 分钟 9.3 万次 / 日志 240MB）→ `CacheRetryPolicy` 退避 500ms×2^(n-1) 封顶 30s＋连续无进展三次终止＋失败页 | ✅ 已完成（2026-09-22 PR #204 三项必需检查全绿 squash 合入 master `ed67a9d`；沙箱 `-UserScope` 同构形态复现-验证：基线三征象全复现→修复版升级完成（旧链移除 / 新链落位 / 凭据更新 / exit 0）＋重试终止取证；本地 930 项全绿净增 7；三任 worker 接力（重启 / 限额各杀一任）；**真机 per-machine 复跑走查转 `待验收`**，[#171](https://github.com/marci-labs/LabelFrame/issues/171)） |
| 96 | PDA 插件机制——`.lfplugin` 跨端契约与 AndroidHost 外置插件通道（双档模型，决策 #157：SDK 档 Zebra 维持内置＋轻量档纯托管插件运行时动态加载；manifest 可选字段 `platforms`（无字段按 Windows 解释，向后兼容存量包零迁移）＋安装端平台门＋服务端 `plugin-packages` 分发跨端语义；Android 安装目录 `{FilesDir}/plugins/<pluginId>/` 一插件一目录（卸载=删目录+重启生效，对齐 Windows）；`PluginInstaller` 自 WinHost 下沉 Core 双端共用；AndroidHost `PluginHost` 装配＋品牌路由＋本地 HTTP 插件端点（与 WinHost 同构）；配置页「插件管理」子页＋「连接打印机」品牌选项卡动态扩展（配置结构零变更，决策 #95 预埋兑现）；指令集分级登记（PostScript 不做、CPCL/ESC-POS 占位、TSPL=迭代 97）） | ✅ 已完成（2026-09-22 PR #205 三项必需检查全绿 squash 合入 master `74d5a43`（rebase 时决策号因并行撞号自 #156 顺延为 #157）；本地 build 0 警 0 错 / test 942 项全绿（排除 Perf/Soak，新增 fake 插件单测矩阵 19 项、WinHost 下沉 28 用例零回归）；沙箱实证 fake 包构建＋Release AOT APK 22.8MB；**AC-02 Spike 真机实证与 AC-04 插件管理真机走查转 `待验收`**（恢复条件 = PDA 真机 UROVO DT50 同级可得，走查脚本已备齐），[#185](https://github.com/marci-labs/LabelFrame/issues/185)）→ ✅ 验收结项（2026-09-23 真机走查通过 + 决策 #159 插件 API 面约束：AC-02 按 fail-fast 双出口第二出口「fail-fast 形态达成」——首轮机制四段（安装→重启装配→动态加载 Loaded=true→品牌路由＋卸载语义）真机通过、发送段 interpreter BCL 缺口（.NET 6+ `ReplaceLineEndings`）如实记录，用户拍板方案 A 约束面先行（保守 API 面写入 DESIGN §6.8 / 决策 #159，方案 B 调宿主 AOT 配置记 §7 未决）；约束面版 fake 复跑全链含发送段通过（测试打印 Completed 1/1＋`[FAKE] 模拟发送…38451 字符`，零 BCL 缺口）；AC-04 全链真机走查通过（沙箱 plugin-packages 浏览→UI 安装→品牌选项卡扩展→配置→测试打印「✓ 打印成功（1/1）」→重启后卸载干净 `installed=[]`＋品牌回退 Zebra，截图 17 张＋logcat 留证）；走查脚本缺陷修复 PR #212；AC 终态 AC-01/02/03/04/05 全 ✅，[证据](https://github.com/marci-labs/LabelFrame/issues/185#issuecomment-5787771580)；用户委托自动结项（2026-09-11 分工协议），[#185](https://github.com/marci-labs/LabelFrame/issues/185)） |
| — | 验收取证：升级链卸载清理真机复验（#171 返修后复跑，PR #204 三缺陷四组断言＋#171 原始两观察点）——v0.28.0 首装立旧链（ARP 双条 / 常量 CacheId 缓存目录 / 截断桩缓存 EXE / 凭据 0.28.0 / healthz 200）→ 0.29.99（master `ed67a9d` 构建修复版引导 EXE）覆盖升级真实向导走查 | ✅ 已完成（2026-09-23 LF-Accept-Win10 真机四组断言全绿：①升级全程零 `0x80091007`、版本化缓存目录 `WebUiPlacement.0.29.99` / `WebUiPlacementCleanup.0.29.99` 成套落位；②提权桩会话内嵌容器载荷全量获取校验、零 `WixAttachedContainer 0x80070002`；③净结果——旧 Bundle 链尾卸载真实执行（旧卸载会话日志在案＋「新先旧后」序直接证据：旧 BA 链尾卸载时凭据已 0.29.99 判「webui 在位 = 否」未误清）、ARP 0.28.0 双条目消失、凭据 0.28.0 → 0.29.99、服务 Running / healthz 200；④死源场景 11 秒终止＋日志 0.031MB 有界＋失败页呈现＋回滚干净零悬挂 ARP；证据 [评论](https://github.com/marci-labs/LabelFrame/issues/171#issuecomment-5781558411)；用户委托自动结项（2026-09-11 分工协议），[#171](https://github.com/marci-labs/LabelFrame/issues/171)） |
| 100 | 依赖升级：TemplateFrame.Excel.Simple 2.0.0 → 2.4.1（Excel 导入容错与读性能，决策 #158）——全仓唯一引用点 Core.csproj，产品代码零改动；受益：2.2.0 修复「表头不在 A 列起始的第三方表格错读（前几列恒空、末列静默丢弃）」＋漏裸 XmlException、2.4.0 读性能优化（行 / 单元格索引复用）；传递依赖 DocumentFormat.OpenXml 钉 3.3.0 无冲突；配套回归测试补强 2 项（续填行识别——命名区域下方 inline string 续填 2 行全识别，防上游回退；非 A 列起始表头第三方表格无丢列，SimpleExcel.Read / ExcelTableReader 双层断言）；上游新能力接入不在范围（如需另行立项） | ✅ 已完成（2026-09-23 记账随实现 PR 同交；本地 Release 构建 0 警 0 错 / dotnet test 全绿含新增回归测试；**AC-05 真机「下载模板→续填→导入」冒烟转 `待验收`**（恢复条件 = LF-Accept-Win10 或用户环境实际跑一轮冒烟），[#210](https://github.com/marci-labs/LabelFrame/issues/210)） |
| 101 | Linux 离线部署包——Release 新增单附件 `labelframe-offline-<版本>-linux-x64.tar.gz`：镜像 tar（`docker save`，ghcr 全名 tag 原样保留——load 后本地命中、compose 不再联网拉取）＋与在线版逐字节同源的 compose.yml ＋钉定 .env ＋ README / install.sh（SHA256SUMS 强制校验 fail-closed）＋ packages 三件（Client MSI / PDA APK / .lfplugin，拷入挂载目录即经下载中心分发）＋管理界面预解压（开箱可用）；release.yml 新增 offline-bundle job 组包＋结构断言＋**真离线自验**（无镜像 runner 以包内 install.sh 走用户全链，不可用即发版失败）；修订决策 #64「Release 不含 docker 离线包」（决策 #159）；不进 install-manifest | ✅ 已完成（2026-09-23 记账随实现 PR 同交；AC-02 本地 Docker 全链演练通过（构建→组包→篡改拒装→load / up / healthz / 管理界面→幂等重跑），证据见 Issue；**AC-04 发版流水线全链与 AC-06 用户离线环境走查转 `待验收`**（恢复条件 = 下一版 `v*` tag 发版 / 真实离线环境可得），[#213](https://github.com/marci-labs/LabelFrame/issues/213)） |
| 待需求 | 兼容与扩展（net48 / WMS 模板下发 / TSPL / 统计 / 契约 Pattern 校验） | 待定 |

## 待需求（有真实需求再排）

> 2026-09-10 迭代 44 验收后用户提出五项候选，已全部交付：批次首张节奏 / 设置页布局 / 工作台搜索与操作列（迭代 45，[#11](https://github.com/marci-labs/LabelFrame/issues/11)）、缩略图预览（迭代 46，[#15](https://github.com/marci-labs/LabelFrame/issues/15)）、进度增量上报与日志细化（迭代 47，[#16](https://github.com/marci-labs/LabelFrame/issues/16)）；工作台整体信息架构重构亦随候选讨论定稿排期（迭代 48，[#27](https://github.com/marci-labs/LabelFrame/issues/27)，卡片网格形态交付）。

- net48 版 WinHost（Win7 / Win8 老电脑，尽量兼容）。
- `ci.yml` / `release.yml` 的 actions 升级（checkout@v7 / setup-dotnet@v6，同款 Node 20 弃用警告清理——迭代 99 只动了 nightly-perf.yml，门禁 / 发版链路敏感，另行小迭代）。
- WMS 模板下发（复用模板包格式）。
- 其他打印机指令集（TSPL / CPCL）。
- PDA 蓝牙传输（原 P1；2026-09-07 Niimbot 专用插件迭代已放弃而降级——PDA 宿主走 IP 打印、不依赖蓝牙，出现真实蓝牙打印机需求时按传输插件接入）。
- 打印历史统计。
- 多打印机并行。
- 界面可用性低优先项（2026-09-17 界面评审 [#114](https://github.com/marci-labs/LabelFrame/issues/114) 终版排期降级；高/中优先项已立迭代 80–85：#128 / #129 / #130 / #131 / #132 / #133）：
  - 作业状态「已领取」补用户解释（谁来领取、要等多久；A-4）。
  - 批次打印间隔单位毫秒改秒（「什么时候需要开启」的说明已存在；A-5 复核后仅剩单位项）。
  - 设计器「码制 / 模块宽 / 打印 DPI」术语加解释或折叠进高级区（B-8）。
  - 下载中心「客户端下载（PC）」分区按适用端 / 类型过滤并标「最新」（B-10）。
  - 「已是最新」与旧安装包并存时下载抑制 / 降级警示（B-11；2026-09-17 只读实证真实服务端 0.24.0 旧包与本机 0.27.1 并存）。
  - 客户端「插件管理 / 更新与安装包」提示补服务端管理界面入口或地址链接（C-6）。
- 契约字段 Pattern 校验（迭代 1 仅存元数据未执行；2026-08-17 列为未来事项，现阶段不处理）。

---

> 未闭环的真机 / 用户验收欠账：[ACCEPTANCE-BACKLOG.md](ACCEPTANCE-BACKLOG.md)（存量索引；迭代 42 起新增欠账由打 `待验收` 标签的 Issue 追踪）。
