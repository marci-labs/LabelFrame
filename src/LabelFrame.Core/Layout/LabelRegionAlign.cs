namespace LabelFrame.Core.Layout;

/// <summary>元素在区域（格子）内的对齐方式。</summary>
public enum LabelRegionAlign
{
    /// <summary>起始（左上）。</summary>
    Start,

    /// <summary>居中。</summary>
    Center,

    /// <summary>末尾（右下）。</summary>
    End,
}